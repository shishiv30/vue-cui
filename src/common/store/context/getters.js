const viewport = state => state.viewport;
const urls = state => state.urls;
const location = state => state.location;
const userAgent = state => state.userAgent;
export default {
    viewport,
    urls,
    location,
    userAgent
};
